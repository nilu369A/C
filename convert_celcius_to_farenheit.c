#include <stdio.h>

int main()
{
        // write a c program to convert celcius to farenheit
        float celcius = 12, farenheit;
        farenheit = (celcius * 9 / 5) + 32;
        printf("The value of celcius temerature in farenheit is %f", farenheit);
        return 0;
}