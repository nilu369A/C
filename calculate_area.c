#include <stdio.h>

int main()
{
        // Write a c program to calculate the area of a retangle

        int height = 5, width = 7;
        int area = height * width;

        printf("The area of the rectangle is %d", area);
        return 0;
}